<%= render 'shared/page_title', title: "Editar Categoría" %> 
<%= render 'shared/errors', obj: @car %>
<div class="row">
    <div class="well  col-md-8 col-md-offset-2">
      <%= form_for @car do |f| %>
      <%= f.label :Nombre %>
      <%= f.text_field :name %>
  
      <%= f.submit 'Editar Categoría',class:"btn btn-danger"%>
      <%end%>
    </div>
  </div>
  